import React from 'react';

class Tabs extends React.Component {
    constructor (props) {
        super(props)
        this.state = {selected: 0}
    }

    // titleLis = props.map((prop, i) => {
    //     <li>
    //         <header onClick={ }><h1 key={i} >{prop.title}</h1></header>
            
    //     </li>
    // })

    updateSelected(){
        // this.setState({selected: })
    }

    render() {

        return (
            <div>
                {/* <ul>{ titleLis }</ul> */}
            </div>
        )
    }
}

export default Tabs;